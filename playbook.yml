---
 - name: crete users
   hosts: all
   become: yes
   remote_user: imene
   roles:
     - user
  #Apache 
 - name: install serveur web 
   hosts: webservers
   roles:
    - web
   #Dtabase
 - name: install DataBase
   hosts: db
   roles:
    - database
